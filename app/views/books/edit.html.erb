<% if flash[:notice] %>
    <div class = "flash">
        <%= flash[:notice] %>
    </div>
<% end %>



<% if @book.errors.any? %>
    <div class = "error_explanation">
      <h3><%= @book.errors.count %>errors prohibited this book from being saved:</h3>
      <% @book.errors.full_messages.each do |message| %>
      <ul>
        <li><%= message %></li>
      </ul>
      <% end %>
    </div>

<% end %>

<h1>Editing book</h1>

<%= form_for(@book) do |f| %>
    <h4>title</h4>
    <%= f.text_field :title %>
    <h4>body</h4>
    <%= f.text_area :body %><br>
    <%= f.submit "Update Book" %>
<% end %>

<div class = "next-action"> <%= link_to "Show", book_path %></div>
|
<div class = "next-action" ><%= link_to "Back", books_path %></div>